package newBJ.calculation

object HandCalculator {

  //  def findFutureHand(hand: Hand, deck: Deck): Seq[Hand] =
  //    if(17 <= hand.sum) Seq(hand)
  //    else {
  //      for{
  //        d <- deck
  //        newHand = hand + d
  //        exchangeHand = newHand.exchangeAce((17 to 21).contains)
  //        f <- findFutureHand(exchangeHand, deck diff Seq(d))
  //      } yield f
  //    }
}
